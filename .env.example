# ============================================================
# Robi Backend — Variables de Entorno
# Copia este archivo a .env y completa los valores reales.
# ¡NUNCA subas el .env real al repositorio!
# ============================================================

# ── Servidor ────────────────────────────────────────────────
HOST=0.0.0.0
PORT=9393
ENVIRONMENT=development        # development | production
SERVER_IP=192.168.2.200        # IP local del servidor (para TLS / CORS)

# ── WebSocket ───────────────────────────────────────────────
WS_PING_INTERVAL=30            # segundos entre pings keepalive
WS_PING_TIMEOUT=10             # segundos hasta declarar conexión muerta
WS_MAX_MESSAGE_SIZE_MB=50      # tamaño máximo de mensaje binario (audio)

# ── Seguridad ───────────────────────────────────────────────
API_KEY=change-me-generate-a-random-32-char-string
ALLOWED_ORIGINS=https://192.168.2.200  # origenes permitidos para CORS

# ── LLM — Gemini ────────────────────────────────────────────
GEMINI_API_KEY=your-google-ai-studio-api-key-here
GEMINI_MODEL=gemini-2.0-flash-lite
GEMINI_MAX_OUTPUT_TOKENS=512
GEMINI_TEMPERATURE=0.7

# ── Conversación ────────────────────────────────────────────
CONVERSATION_KEEP_ALIVE_MS=60000       # ms de escucha continua tras interacción
CONVERSATION_COMPACTION_THRESHOLD=20   # compactar cada N mensajes (resumen msgs 1-15)

# ── Búsqueda de persona ─────────────────────────────────────
PERSON_SEARCH_TIMEOUT_MS=8000          # ms máximos para buscar persona tras wake word

# ── Base de Datos ────────────────────────────────────────────
DATABASE_URL=sqlite+aiosqlite:///./data/robot.db

# ── Almacenamiento ───────────────────────────────────────────
MEDIA_DIR=./media
MAX_UPLOAD_SIZE_MB=50

# ── Logging ──────────────────────────────────────────────────
LOG_LEVEL=INFO                         # DEBUG | INFO | WARNING | ERROR
LOG_FILE=./media/logs/robot.log
